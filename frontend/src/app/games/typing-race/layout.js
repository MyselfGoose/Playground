export const metadata = {
  title: "Typing Race — Playground",
  description: "Monkeytype-inspired typing test: speed, accuracy, flow.",
};

export default function TypingRaceLayout({ children }) {
  return children;
}
